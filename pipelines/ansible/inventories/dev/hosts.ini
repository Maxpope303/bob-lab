# inventories/dev/hosts.ini
# Development inventory — replace IP addresses with actual hosts or use
# dynamic inventory plugins (AWS EC2, GCP, etc.) for cloud environments.

[twas_servers]
# twas-dev-01 ansible_host=10.0.1.10

[liberty_servers]
# liberty-dev-01 ansible_host=10.0.1.20

[all:vars]
ansible_user=ansible
ansible_python_interpreter=/usr/bin/python3
